
* Try fixing the size artifacts by triggering a resize on new game and unpause [done but doesn't work on new game]

* Find a consistent way of including constants. Many are in the constants module but some are classvars. I think any constant that is only used in one class
    should be a classvar and any constant used in multiple places should be a global variable in the constants module.

* Settings json should not be checked into git repo. It should instead but a settings.json into ~/.config/QMines (look up macos and win analogues).

* Do the time tracker.

* Refactor, make code prettier, order the methods appropriately, config and run ruff, make things a bit more elegant

* Add dialogue for game over (win or loss)